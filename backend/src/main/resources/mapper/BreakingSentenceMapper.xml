<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper
        PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
        "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.yunting.mapper.BreakingSentenceMapper">

    <resultMap id="BreakingSentenceResultMap" type="com.yunting.model.BreakingSentence">
        <id property="breakingSentenceId" column="breaking_sentence_id"/>
        <result property="taskId" column="task_id"/>
        <result property="originalSentenceId" column="original_sentence_id"/>
        <result property="content" column="content"/>
        <result property="charCount" column="char_count"/>
        <result property="sequence" column="sequence"/>
        <result property="synthesisStatus" column="synthesis_status"/>
        <result property="audioUrl" column="audio_url"/>
        <result property="audioDuration" column="audio_duration"/>
        <result property="ssml" column="ssml"/>
        <result property="jobId" column="job_id"/>
        <result property="createdAt" column="created_at"/>
        <result property="updatedAt" column="updated_at"/>
    </resultMap>

    <insert id="insertBatch" parameterType="java.util.List">
        INSERT INTO breaking_sentences (task_id, original_sentence_id, content, char_count, sequence,
                                       synthesis_status, audio_url, audio_duration, ssml, created_at, updated_at)
        VALUES
        <foreach collection="list" item="item" separator=",">
            (#{item.taskId}, #{item.originalSentenceId}, #{item.content}, #{item.charCount}, #{item.sequence},
             #{item.synthesisStatus}, #{item.audioUrl}, #{item.audioDuration}, #{item.ssml}, NOW(), NOW())
        </foreach>
    </insert>

    <select id="selectByTaskId" parameterType="long" resultMap="BreakingSentenceResultMap">
        SELECT * FROM breaking_sentences
        WHERE task_id = #{taskId}
        ORDER BY sequence ASC
    </select>

    <select id="countByTaskId" parameterType="long" resultType="int">
        SELECT COUNT(1) FROM breaking_sentences WHERE task_id = #{taskId}
    </select>

    <select id="selectPageByTaskId" resultMap="BreakingSentenceResultMap">
        SELECT *
        FROM breaking_sentences
        WHERE task_id = #{taskId}
        ORDER BY sequence ASC
        LIMIT #{limit} OFFSET #{offset}
    </select>

    <select id="selectById" parameterType="long" resultMap="BreakingSentenceResultMap">
        SELECT * FROM breaking_sentences WHERE breaking_sentence_id = #{breakingSentenceId}
    </select>

    <delete id="deleteById" parameterType="long">
        DELETE FROM breaking_sentences WHERE breaking_sentence_id = #{breakingSentenceId}
    </delete>

    <update id="decrementSequenceAfter">
        UPDATE breaking_sentences
        SET sequence = sequence - 1
        WHERE task_id = #{taskId}
          AND sequence > #{sequence}
    </update>

    <select id="selectByIds" resultMap="BreakingSentenceResultMap">
        SELECT *
        FROM breaking_sentences
        WHERE breaking_sentence_id IN
        <foreach collection="ids" item="id" open="(" separator="," close=")">
            #{id}
        </foreach>
        ORDER BY sequence ASC
    </select>

    <select id="selectPendingByTaskId" parameterType="long" resultMap="BreakingSentenceResultMap">
        SELECT *
        FROM breaking_sentences
        WHERE task_id = #{taskId}
          AND synthesis_status != 2
        ORDER BY sequence ASC
    </select>

    <update id="updateSynthesisInfo">
        UPDATE breaking_sentences
        SET synthesis_status = #{status},
            audio_url = #{audioUrl},
            audio_duration = #{audioDuration},
            updated_at = NOW()
        WHERE breaking_sentence_id = #{breakingSentenceId}
    </update>

    <update id="resetSynthesisStatus">
        UPDATE breaking_sentences
        SET synthesis_status = 0,
            audio_url = NULL,
            audio_duration = NULL,
            ssml = ssml,
            updated_at = NOW()
        WHERE breaking_sentence_id = #{breakingSentenceId}
    </update>

    <update id="updateContent">
        UPDATE breaking_sentences
        SET content = #{content},
            char_count = #{charCount},
            updated_at = NOW()
        WHERE breaking_sentence_id = #{breakingSentenceId}
    </update>

    <update id="updateSsml">
        UPDATE breaking_sentences
        SET ssml = #{ssml},
            updated_at = NOW()
        WHERE breaking_sentence_id = #{breakingSentenceId}
    </update>

    <select id="selectByJobId" parameterType="string" resultMap="BreakingSentenceResultMap">
        SELECT * FROM breaking_sentences WHERE job_id = #{jobId} LIMIT 1
    </select>

    <update id="updateJobId">
        UPDATE breaking_sentences
        SET job_id = #{jobId},
            updated_at = NOW()
        WHERE breaking_sentence_id = #{breakingSentenceId}
    </update>
</mapper>

